import * as React from "react";
import { Card, CardContent, CardHeader } from '@mui/material';

export default () => (
    <Card>
        <CardHeader title="FarmData Dashboard" />
        <CardContent>Contents</CardContent>
    </Card>
);